-- ansible-vault.nvim plugin entry point
-- This file is automatically loaded by Neovim

if vim.g.loaded_ansible_vault then
  return
end
vim.g.loaded_ansible_vault = true

-- Plugin will be initialized when setup() is called
